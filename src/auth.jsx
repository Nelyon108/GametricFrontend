export const getUserRole = () => {
    // This function should return the role of the authenticated user.
    // For now, we're returning 'admin' for simplicity.
    return 'admin'; // Change to 'user' for non-admins
  };
  